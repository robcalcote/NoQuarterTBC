@model IEnumerable<NoQuarterTBC.Models.GameRole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GameRoleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GameRoleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GameRoleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GameRoleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GameRoleID })
        </td>
    </tr>
}

</table>
