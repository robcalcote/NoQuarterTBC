@model IEnumerable<NoQuarterTBC.Models.Profession>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProfessionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Primary)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProfessionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Primary)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProfessionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProfessionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProfessionID })
        </td>
    </tr>
}

</table>
