@model IEnumerable<NoQuarterTBC.Models.BankContribution>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.players.PlayerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContributionItem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContributionQuantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContributionDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.players.PlayerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContributionItem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContributionQuantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContributionDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContributionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContributionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContributionID })
        </td>
    </tr>
}

</table>
