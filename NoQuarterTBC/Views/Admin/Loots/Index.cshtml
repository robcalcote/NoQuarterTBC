@model IEnumerable<NoQuarterTBC.Models.Loot>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.bosses.BossName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LootName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.bosses.BossName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LootName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LootID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LootID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LootID })
        </td>
    </tr>
}

</table>
