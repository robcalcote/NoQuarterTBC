@model IEnumerable<NoQuarterTBC.Models.RaidInstance>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.raids.RaidName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RaidDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Progression)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RaidCleared)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RaidRunTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.raids.RaidName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RaidDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Progression)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RaidCleared)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RaidRunTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RaidInstanceID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RaidInstanceID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RaidInstanceID })
        </td>
    </tr>
}

</table>
