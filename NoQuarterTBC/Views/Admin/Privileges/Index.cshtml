@model IEnumerable<NoQuarterTBC.Models.Privilege>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.roles.RoleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RightsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.roles.RoleName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RightsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RightsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RightsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RightsID })
        </td>
    </tr>
}

</table>
